"use strict";
describe('Data Type', function () {
    it('it should declare', function () {
        let name = 'Iqbal Pamula';
        let balance = 1000000;
        let isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
